[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-server"
version = "0.1.0"
description = "Room server — HTTP room management API"
license = {text = "MIT"}
requires-python = ">=3.8"
